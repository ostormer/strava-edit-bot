# Local dev server (npm run dev).
# Vite proxy rewrites /api/* to localhost:5001, so base URL stays empty.
VITE_API_BASE_URL=

# Strava OAuth — client_id is public (safe to commit). Set your app's client_id here.
VITE_STRAVA_CLIENT_ID=217761
